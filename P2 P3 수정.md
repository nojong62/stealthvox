조사 결과 확인했다.

다음 기준으로 P2 Sentence Morphing 방향을 확정한다.

# 1. §17은 A안으로 확정

P2에서는 Breath Echoing을 기본 기능뿐 아니라 선택적 Practice 버튼으로도 넣지 않는다.

P2의 역할을 명확하게:

**Sentence Morphing**

으로 한정한다.

P3에서:

* Breath Echoing
* Full Echo
* Full Shadow
* Recording

을 담당하므로 P2에 별도 말하기 Practice를 다시 넣어 역할을 중복시키지 않는다.

---

# 2. 기존 Breath Echoing 코드는 버리지 않는다

`breath_echoing_engine.dart`

는 삭제하지 않는다.

P2 연결만 제거하고 향후 P3의 Breath Echoing 공통 엔진으로 재사용한다.

P2와 P3가 같은 말하기 기능을 중복 구현하지 않는다.

---

# 3. practice_replay_builder.dart

P2에서 Practice Replay를 사용하지 않게 되므로 현재 실제 사용처를 재확인한다.

P3 설계에서도 필요하지 않다면 dead code로 남기지 말고 삭제한다.

“이미 만들었으니 남긴다”는 이유로 사용하지 않는 코드를 유지하지 않는다.

삭제 여부와 근거를 구현 보고에 포함한다.

---

# 4. 실제 History ladder 10세트

지금 기기에서 방에 들어가 유료 시간을 소비하면서 10세트를 수집하지 않는다.

현재 순수 Dart LCS 프로토타입이 합성/구조변형 예문 11개에서 기대대로 작동한 결과를 기준으로 우선 구현한다.

실제 History ladder 검증은 구현 후 QA 게이트로 둔다.

나중에 내가 기존 P2 화면 문장들을 캡처 또는 텍스트로 제공하면:

Previous
Current
Detected Morph

형태로 실제 10세트를 별도 검증한다.

실제 데이터 검증 전에는 LCS 파라미터를 제품 최종값이라고 과도하게 확정하지 않는다.

---

# 5. 로컬 Diff 방식으로 확정

현재는 A:

**Pure Local Diff**

로 구현한다.

* API 호출 0
* Firestore 변경 0
* 기존 History 모두 지원
* 새 dependency 0

LLM Morph Metadata와 Hybrid는 지금 구현하지 않는다.

실제 History 검증에서 반복적으로 부자연스러운 변화 영역이 발견될 때만 다시 검토한다.

---

# 6. MorphChange 단일 데이터 원칙

하나의 MorphChange 결과를:

1. 화면 highlight
2. TTS 강조 instruction
3. TTS cache identity

세 군데가 공유한다.

각각 별도로 diff 계산하지 않는다.

개념:

MorphChange

* text
* ranges
* phrases
* identity

UI와 Voice의 강조 대상이 서로 달라질 수 없게 한다.

---

# 7. P2 첫 문장

첫 문장은 비교 대상이 없다.

따라서:

전체 문장 기본 표시
→ Smooth Jazz로 자연스럽게 전체 재생

강조 없음.

---

# 8. Sentence 2 이후

각 현재 문장은 바로 이전 문장과 비교한다.

Previous N-1
→ Current N

에서 달라진 부분을 계산한다.

현재 문장 전체를 표시하되:

**이번 단계에서 새로 추가되거나 변형된 부분만 은은하게 강조**

한다.

---

# 9. 단순 Append가 아님

반드시 다음을 처리한다.

Added

예:
I was surprised.
→ I was honestly a little surprised at first.

Changed / Shifted

예:
I didn't like it.
→ I wasn't sure I liked it.

Extended

예:
I thought it was strange.
→ I thought it was strange, but after a while I got used to it.

Reframed

예:
so I wasn't sure...
→ and for a moment, I wasn't quite sure...

사용자에게 Added / Shifted 등의 문법적 분류명은 보여주지 않는다.

사용자는 변화된 부분만 본다.

---

# 10. 강조 개수

한 문장에서 변화 영역이 지나치게 많아지지 않게 한다.

현재 프로토타입의:

* maxRegions = 3
* floodRatio
* suppressRatio

구조를 사용할 수 있다.

단 실제 History 10세트 검증 전까지 수치를 최종 제품 정책으로 간주하지 않는다.

문장 대부분이 바뀐 경우 화면 전체를 강조하지 않는다.

필요하면 강조를 생략한다.

---

# 11. P2 Voice

기본은 Smooth Jazz를 사용한다.

전체 문장은 자연스럽게 연결해서 읽는다.

Sentence 2부터는 MorphChange.phrases를 같은 TTS instruction에 추가해서:

**이번에 달라진 부분을 자연스럽게 조금 더 살려 읽는다.**

단:

* 해당 phrase를 따로 끊어 읽지 말 것
* 과장하지 말 것
* Sing-Song처럼 만들지 말 것
* 문장 전체 흐름을 유지할 것

을 명확히 한다.

목표는 사용자가:

“이번에 저 부분이 달라졌구나.”

를 귀로도 느끼는 정도다.

---

# 12. TTS 구조

변화 phrase마다 별도 TTS를 만들지 않는다.

현재 Sentence 전체 TTS 1회만 생성한다.

Sentence 2의 예:

Current:
At first, I wasn't sure I liked the idea.

Morph:
At first
wasn't sure I liked

이면 TTS는 전체 문장을 한 번 생성하되 instruction에 해당 phrase를 subtle emphasis하도록 전달한다.

---

# 13. UI 흐름

P2 한 단계:

현재 Sentence 화면 표시
→ 약간의 자연스러운 전환
→ Morph 영역 활성화
→ AI 전체 문장 재생
→ 재생 종료 후 짧은 여유
→ 다음 Sentence

로 한다.

복잡한 text morph animation은 만들지 않는다.

새 dependency 추가 금지.

Flutter 기본 fade / TextSpan 정도로 충분하다.

---

# 14. P2에서는 마이크 제거

Sentence Morphing 기본 흐름에서는:

* microphone 없음
* VAD 없음
* User recording 없음
* STT 없음
* User Echo 없음
* Shadowing 없음
* Practice Replay 없음

으로 한다.

P2는 눈 + 귀 단계다.

P3가 입 + 귀 단계를 담당한다.

---

# 15. 기존 “한 번 말하기 / 두 번 말하기”

이 UI와 시작 gate는 P2 Sentence Morphing에서는 의미가 없어진다.

현재 코드를 조사해서 P2에서 제거 또는 대체한다.

억지로 기존 선택을 남기지 않는다.

P2는 진입하면 Sentence Morphing을 자연스럽게 시작하는 방향을 우선한다.

단 기존 화면 진입/과금/세션 시작 구조를 깨지 않도록 정확한 제거 범위를 보고 후 수정한다.

---

# 16. 자동 진행

P2는 기본 자동 진행으로 한다.

Sentence 1
→ AI

Sentence 2
→ Morph highlight + AI

Sentence 3
→ Morph highlight + AI

...

Final
→ Morph highlight + AI
→ P2 완료

너무 빠른 전환이 되지 않도록 AI 종료 후 짧은 여유만 둔다.

실기기에서 조정 가능하게 한다.

---

# 17. 사용자가 다시 듣고 싶은 경우

현재 P2 UI에 replay 또는 이전 줄 접근 수단이 있다면 최대한 재사용한다.

새로운 복잡한 navigation을 만들지 않는다.

자동 진행 중 현재 Sentence를 다시 듣기 위한 최소 기능이 필요한지 조사해서 제안한다.

---

# 18. cache identity

MorphChange.identity를 반드시 TTS cache identity에 포함한다.

같은 text라도 강조 대상이 다르면 다른 TTS로 취급한다.

반대로:

같은 text

* 같은 Smooth Jazz
* 같은 voice
* 같은 instructionVersion
* 같은 Morph identity

이면 cache를 재사용한다.

---

# 19. cedar

cedar는 현재 임시 테스트 Voice일 뿐이다.

제품 최종 Voice로 확정하지 않는다.

한 설정 위치에서만 관리한다.

실제 Sentence Morphing을 여러 문장에서 들어본 뒤 최종 결정한다.

---

# 20. 기존 P2 Breath 연결 제거

현재 커밋 `98e30f6b`로 들어간 P2 Breath 연결 중:

* `_startBreathEcho` 자동 호출
* P2 Breath용 mic/VAD
* Practice Replay 연결
* P2 Breath billing flag
* P2 Breath 상태 표시

등 P2 Sentence Morphing에서 필요 없는 연결을 제거한다.

단 `breath_echoing_engine.dart` 자체는 P3 재사용을 위해 유지한다.

불필요한 dead code는 남기지 않는다.

---

# 21. 기존 구 Shadowing dead code

기존:

* `_startShadowAiVoice`
* `_startShadowRecording`

등이 더 이상 P2/P3 어느 곳에서도 사용되지 않는 것이 확실하면 삭제한다.

단 삭제 전에 전체 참조를 다시 확인한다.

---

# 22. 구현 파일

우선 예상:

신규:

* `services/sentence_morph.dart`
* `test/sentence_morph_test.dart`

수정:

* `chat_history_master.dart`

필요 없어진 P2 전용 코드 정리.

새 dependency 없음.

Firestore/Firebase schema 변경 없음.

---

# 23. 테스트

프로토타입에서 검증한 최소 11개 케이스를 실제 unit test로 고정한다.

특히 반드시 포함:

* 단순 추가
* 앞부분 추가
* 중간 단어 추가
* 표현 교체
* suffix extension
* prefix reframing
* 여러 곳 동시 변화
* so → and for a moment 같은 구조 변화
* 한 단어 변경
* 대부분 재작성
* punctuation/apostrophe

추가로:

* range overlap 없음
* range가 text 범위를 넘지 않음
* 최대 region 제한
* identity deterministic

을 검증한다.

---

# 24. 구현 순서

1. sentence_morph 순수 함수 + 테스트
2. 현재 P2 Breath 자동 경로 제거
3. P2 시작 gate 정리
4. Text → RichText/TextSpan
5. MorphChange 연결
6. Dynamic TTS emphasis instruction
7. Morph cache namespace
8. 자동 line 진행
9. Billing 단순화
10. cleanup 정리
11. analyze/test
12. 실기기 검증

---

# 25. 구현 후 실기기 검증

실제 P2에서 최소 여러 ladder를 본다.

각 transition마다 확인:

* 강조 위치가 자연스러운가
* 너무 많은 부분이 강조되지 않는가
* 구조가 바뀐 부분을 제대로 잡는가
* AI가 해당 부분을 정말 살짝 더 강조하는가
* 강조가 과하지 않은가
* 자동 진행 속도가 적당한가
* P2가 P3와 역할상 확실히 다르게 느껴지는가

이후 실제 History 10세트의 diff를 별도 검증한다.

---

# 26. P2/P3 최종 역할

P2:

**Sentence Morphing**
문장이 어떻게 자라고 형태가 바뀌는지 눈과 귀로 체감.

P3:

**Speaking Practice**
Breath Echoing
→ Full Echo
→ Full Shadow
→ Recording.

이 역할을 섞지 않는다.

---

이 기준으로 이제 P2 Sentence Morphing 실제 구현에 들어가라.

구현 후 결과만 보고한다.

커밋하지 마라.
빌드하지 마라.
푸시하지 마라.
배포하지 마라.


# StealthVox — P3 Speaking Practice 구현 지시문

이제 P3를 실제 Speaking Practice 단계로 재구성한다.

P2는 Sentence Morphing으로 역할이 확정됐다.

따라서 P3는 최종 완성 문장 하나를 대상으로 실제 말하기 연습을 담당한다.

최종 구조:

**Breath Echoing**
→ **Full Echo**
→ **Full Shadow**
→ **AI / Echo / Shadow 비교 재생**

이번에는 구조를 먼저 완성한다.

세부 timing, silence, Voice, Shadow gap은 실기기에서 이후 미세 조정한다.

---

# 1. P3의 역할

P3는 최종 완성 문장을 실제로 입으로 익히는 단계다.

P2:

* 문장이 어떻게 성장/변형되는지 보고 듣기

P3:

* 최종 문장을 실제 rhythm과 breath 단위로 말하기
* 전체 문장 혼자 말하기
* AI와 함께 shadowing
* 자신의 결과 다시 듣기

P2와 역할을 섞지 않는다.

---

# 2. P3 기본 흐름

P3에 진입하면 최종 sentence 하나를 기준으로 다음 순서로 진행한다.

## Stage 1 — Breath Echoing

AI Breath 1
→ AI 완전 종료
→ User Echo

AI Breath 2
→ User Echo

...

마지막 Breath
→ User Echo

완료.

↓

## Stage 2 — Full Echo

AI Original 전체 문장 재생
→ AI 완전 종료
→ User가 전체 문장을 혼자 말함
→ User recording 완료

↓

## Stage 3 — Full Shadow

Shadow gap 옵션 선택
→ gap이 추가된 AI Full Sentence 재생
→ User가 AI와 동시에 전체 문장 Shadowing
→ User recording 완료

↓

## Stage 4 — Compare

AI ORIGINAL
ECHO
SHADOW

세 개를 각각 다시 들을 수 있게 한다.

---

# 3. 하나의 Full PCM만 사용

매우 중요하다.

P3 최종 문장에 대해 TTS를 여러 번 생성하지 않는다.

한 번:

final sentence
→ Smooth Jazz
→ gpt-4o-mini-tts
→ response_format pcm
→ 24kHz / PCM16 / mono

Full PCM을 확보한다.

이 PCM 하나에서:

* Breath Echoing segments
* AI Original
* Full Shadow gapped PCM

을 모두 만든다.

추가 TTS 호출 금지.

---

# 4. Smooth Jazz

P3 기본 TTS는 현재 P2/Voice Lab과 같은 Smooth Jazz 정의를 사용한다.

별도 instruction 복사본을 만들지 않는다.

기본:

* Accent: General American
* Smooth, melodic, gently undulating
* Medium speed
* Relaxed, soft, conversational
* Moderate subtle expression

Voice는 현재 임시 Breath voice 설정을 재사용한다.

현재 cedar가 temporary default라면 그대로 사용하되 제품 최종 Voice라고 간주하지 않는다.

Voice 설정은 한 곳에서만 관리한다.

---

# 5. Breath 분석

Full PCM을 기존:

`audio_silence_analyzer.dart`

로 분석한다.

Lab/P2 Breath에서 검증한 동일한 실제 PCM pause 기반 방식이다.

텍스트 punctuation 또는 문법 규칙으로 Breath를 먼저 나누지 않는다.

반드시:

**실제로 생성된 AI 음성의 pause**

를 기준으로 BreathSegment를 만든다.

---

# 6. 기존 Breath Echoing 엔진 재사용

이미 존재하는:

`services/breath_echoing_engine.dart`

를 P3에 실제 연결한다.

P3용으로 동일 기능을 새로 복사 구현하지 않는다.

필요하다면 P3가 요구하는 callback/state만 최소 확장한다.

엔진은 계속 UI 독립적으로 유지한다.

---

# 7. Stage 1 — Breath Echoing

각 Breath마다:

AI Breath 재생
→ playback complete 확인
→ AI 정지
→ User microphone 시작
→ User가 같은 Breath 말함
→ speech end + silence 감지
→ recorder stop
→ 다음 Breath

로 진행한다.

AI와 User가 동시에 말하지 않는다.

---

# 8. Breath Echoing 녹음 의미

P3 Breath Echoing의 각 개별 Breath를 사용자에게 녹음 버튼으로 노출하지 않는다.

Breath 1 ▶
Breath 2 ▶

같은 UI를 만들지 않는다.

Breath Echoing의 목적은:

**전체 문장을 말하기 전에 호흡과 rhythm을 몸에 넣는 것**

이다.

이번 P3에서는 Breath별 Practice Replay를 만들 필요 없다.

따라서 P2에서 삭제한 `practice_replay_builder.dart`를 다시 만들지 않는다.

---

# 9. Breath Echoing User PCM

기존 BreathEchoingEngine이 User PCM을 필요로 한다면 그대로 사용할 수 있다.

다만 P3에서는 Breath별 PCM을 최종 파일로 조립하거나 영구 저장하지 않는다.

각 Breath turn 완료 여부 및 VAD에 필요한 transient 데이터로만 사용한다.

다음 Breath로 넘어가면 필요 없는 PCM은 즉시 해제할 수 있게 한다.

메모리를 불필요하게 쌓지 않는다.

---

# 10. Breath silence

초기에는 현재 BreathEchoTiming 정책을 재사용한다.

현재 후보:

발화 후 trailing silence 약 1500ms.

그러나 제품 최종값이 아니다.

P3 실기기에서 다음을 보고 조정한다.

* 짧은 phrase인데 너무 오래 기다리는가
* 문장 중 자연스러운 pause를 발화 종료로 잘 잡는가
* 사용자가 마지막 단어를 길게 말할 때 잘리지 않는가

P3 전용 timing으로 조정 가능하게 한다.

다른 모드 VAD는 변경하지 않는다.

---

# 11. Breath 무발화

User가 Breath 차례에 전혀 말하지 않을 경우 영원히 기다리지 않는다.

현재 엔진의 no-speech timeout을 재사용하거나 P3 전용 값으로 둔다.

초기 8초 수준을 사용 가능하지만 최종값이 아니다.

timeout 후:

* 해당 Breath를 skip
* 다음 Breath

로 넘어간다.

에러 화면으로 학습 전체를 중단하지 않는다.

---

# 12. Breath Echo UI

현재 final sentence 전체를 화면에 보여준다.

가능하면 현재 Breath만 은은하게 강조한다.

예:

Breath 1/3 · 듣기

AI 재생.

↓

Breath 1/3 · 따라 말하세요

User turn.

↓

Breath 2/3 · 듣기

...

P2 Sentence Morphing의 “변화 강조”와는 시각적 의미가 다르므로 상태가 혼동되지 않게 한다.

과한 animation 금지.

---

# 13. Stage 1 완료

마지막 Breath User Echo가 끝나면 Breath Echoing을 완료한다.

즉시 같은 Breath 과정을 다시 반복하지 않는다.

짧은 transition 후 Full Echo로 이동한다.

---

# 14. Stage 2 — Full Echo

Full Echo 의미:

**AI 전체 문장을 먼저 듣고, AI가 완전히 끝난 뒤 User가 혼자 전체 문장을 말한다.**

흐름:

AI ORIGINAL Full PCM
→ playback complete
→ AI 완전 정지
→ User full recording start
→ User 전체 sentence 발화
→ final silence
→ recording stop
→ ECHO recording ready

AI와 User 음성이 겹치면 안 된다.

---

# 15. Full Echo mic 시작

AI playback complete 후 불필요한 고정 delay를 넣지 않는다.

Voice Lab에서 mic start latency가 약 32ms로 측정됐으므로 기본은 즉시 recorder start.

단 실제 기기에서 AI speaker tail이 녹음에 들어가는 것이 확인될 때만 guard delay를 추가한다.

추측으로 300ms 등을 먼저 넣지 않는다.

---

# 16. Full Echo recording format

Full Echo는 stitching이 필요 없다.

따라서 기존 P3 local recording 방식이 안정적이면:

AAC-LC / m4a

등 기존 DeviceFileSource로 잘 재생되는 형식을 유지해도 된다.

굳이 PCM raw로 저장하지 않는다.

새 dependency 추가 금지.

---

# 17. Full Echo VAD

Full sentence User recording 종료는 기존 P3 또는 Lab에서 검증한 silence detection 로직을 재사용한다.

초기값은 실기기 테스트가 가능하도록 조정 가능한 상수로 둔다.

주의:

Breath Echoing의 짧은 phrase silence와 Full Echo의 전체 sentence silence는 요구가 다를 수 있다.

필요하면 별도 상수로 분리한다.

다른 모드 값 변경 금지.

---

# 18. Full Echo 무발화

AI Original 재생 후 User가 아무 말도 하지 않은 경우:

무발화 timeout
→ recording 폐기
→ ECHO 버튼 비활성

으로 한다.

Voice Lab에서 이미 수정한 동일 원칙을 적용한다.

빈 m4a 파일을 성공한 녹음으로 취급하지 않는다.

---

# 19. Full Echo 결과

정상 녹음이 끝나면:

`ECHO`

재생 버튼 활성화.

사용자가 즉시 자신의 전체 문장을 들을 수 있어야 한다.

자동으로 AI Original → Echo recording을 연속 재생하지 않는다.

각 버튼을 독립적으로 눌러 비교한다.

---

# 20. Stage 3 — Full Shadow

Full Shadow는:

AI가 전체 문장을 읽는 동안 User가 동시에 따라 말하는 단계다.

하지만 원본 AI 음성의 Breath 사이 pause를 그대로 쓰면 따라가기 빡빡할 수 있으므로:

**Breath 사이의 빈 공간을 조금 더 늘린 Shadow 전용 PCM**

을 만든다.

---

# 21. Shadow PCM 생성 방식

기존:

`buildGappedPcm()`

을 재사용한다.

원본 Full PCM의 샘플을 삭제하거나 변형하지 않는다.

Breath boundary마다:

원래 PCM
+
추가 zero PCM silence

만 삽입한다.

즉 TTS 재생성 없음.
재인코딩 없음.

---

# 22. Shadow gap 옵션 3개

사용자가 Full Shadow 전에 3가지 난이도/여유도를 선택할 수 있게 한다.

초기값은 다음으로 구현한다.

## Tight

원래 pause + 300ms

## Normal

원래 pause + 500ms

## Relaxed

원래 pause + 800ms

내부값:

300 / 500 / 800ms

제품 최종 수치는 아니다.

실기기 테스트 후 조정 가능하게 한 곳의 설정으로 관리한다.

---

# 23. Shadow gap UI

사용자에게 밀리초 숫자를 굳이 보여주지 않는다.

우선 제품 표시 후보:

`Tight`
`Normal`
`Relaxed`

또는 현재 앱 언어 스타일에 맞춰:

`빠르게`
`보통`
`여유롭게`

로 표현 가능하게 한다.

내부 enum/value와 UI label을 분리한다.

초기 기본 선택은:

**Normal (+500ms)**

로 한다.

---

# 24. Shadow gap 변경 시 TTS 호출 금지

Tight → Normal → Relaxed를 바꿔도 API를 다시 호출하면 안 된다.

같은 Full PCM과 동일 BreathSegment를 가지고:

buildGappedPcm(fullPcm, segments, selectedGap)

만 다시 수행한다.

로컬 연산뿐이다.

---

# 25. Full Shadow recording 시작 순서

Full Shadow에서는:

User recorder START
→ recorder가 실제 시작된 것 확인
→ AI gapped PCM playback START

순서로 한다.

첫 음절이 녹음에서 잘리지 않게 하기 위함이다.

AI를 먼저 재생하고 recorder를 나중에 켜지 않는다.

---

# 26. Shadow recording 종료

AI gapped playback complete
→ 짧은 tail 여유
→ recorder stop

으로 한다.

Voice Lab에서 사용한 700ms를 초기값으로 사용할 수 있다.

단 제품 최종값은 아니다.

실기기에서 마지막 User syllable이 잘리는지 보고 조정한다.

---

# 27. Shadow 발화 감지

Full Shadow에서는 AI speaker audio가 mic에 들어올 수 있으므로:

“User speech가 감지됐는가”

를 일반 VAD로 판단해서 recording을 폐기하지 않는다.

Voice Lab에서 확인한 것처럼 speaker playback과 User voice를 완벽히 구분할 수 없다.

따라서 Shadow recording은 AI playback 시간 기준으로 정상 완료시키는 것이 기본이다.

---

# 28. Shadow 녹음의 AI leakage

현재 speakerphone 환경에서는 Shadow recording에 AI 음성이 일부 들어갈 수 있다.

이번 구현에서 이를 없애기 위한 별도 DSP/AEC 작업을 하지 않는다.

기존 Android audio configuration이 제공하는 범위만 사용한다.

이 단계의 목적은:

* rhythm
* timing
* 따라가기

를 연습하는 것이다.

녹음이 studio-quality voice sample일 필요는 없다.

---

# 29. Shadow 결과

정상 종료 후:

`SHADOW`

재생 버튼 활성화.

Full Echo recording과 별도 파일로 유지한다.

Echo를 Shadow 파일로 덮어쓰지 않는다.

---

# 30. Stage 4 — 비교 영역

최종적으로 P3 화면에서 최소:

`AI ORIGINAL`
`ECHO`
`SHADOW`

세 가지 재생을 독립적으로 할 수 있게 한다.

각 버튼 의미:

AI ORIGINAL
= Smooth Jazz 원본

ECHO
= AI가 끝난 뒤 User가 혼자 전체 문장을 말한 녹음

SHADOW
= gap이 들어간 AI와 동시에 따라 말한 User 녹음

---

# 31. 자동 비교 재생 없음

AI → Echo → Shadow를 자동으로 연속 재생하지 않는다.

사용자가 원하는 것을 선택해서 반복 비교하게 한다.

동시에 두 AudioPlayer가 재생되지 않게 한다.

한 버튼을 누르면 현재 다른 playback은 stop한다.

---

# 32. P3 상태 머신

P3 상태를 명시적으로 관리한다.

예:

preparing
breathListen
breathEcho
fullEchoListen
fullEchoRecord
shadowReady
fullShadowRecord
compare
stopped

정확한 enum 이름은 현재 코드 스타일에 맞춘다.

여러 bool 조합만으로 stage가 모호해지지 않게 한다.

---

# 33. 사용자의 Back / Exit

P3 중 어느 단계에서 나가더라도 즉시:

* AI playback stop
* User recording stop
* PCM stream cancel
* timers cancel
* Breath engine stop
* generation invalidate

가 되어야 한다.

다음 화면에서 이전 P3 audio가 계속 재생되는 일이 없어야 한다.

---

# 34. temporary 파일

P3에 생성되는:

* Full Echo recording
* Full Shadow recording

은 local temporary only.

방을 나가면 삭제.

History Firestore schema에 저장하지 않는다.

Firebase Storage 업로드 없음.

앱 재실행 후 복원하지 않는다.

---

# 35. 다시 P3 시작

같은 History에서 P3를 다시 시작하면 이전 P3 temporary recording을 정리하고 새 session으로 시작한다.

AI Original TTS cache는 재사용한다.

User Echo/Shadow recording은 새로 만든다.

---

# 36. TTS Cache

Full Smooth Jazz PCM cache에는 최소:

* model
* style/pattern identity
* instruction version
* voice
* final text

가 반영되어야 한다.

P2 Sentence Morphing에서 사용하는 “변화 강조용 TTS”와 P3 Original TTS는 instruction이 다르므로 같은 cache identity를 강제로 공유하지 않는다.

P3 Original은 **Morph 강조 없는 Smooth Jazz 원본**이어야 한다.

---

# 37. P2 TTS와 차이

P2 Sentence Morphing:

Smooth Jazz
+
changed phrase subtle emphasis

P3:

기본 Smooth Jazz 원본.

P3에서는 MorphChange 강조 instruction을 사용하지 않는다.

최종 문장을 특정 단어만 강조한 상태로 학습시키지 않는다.

---

# 38. Billing

P3에서 최소 다음 상태 동안 billing busy가 유지되게 한다.

* Full PCM preparing
* Breath AI playback
* Breath User capture
* Full Echo AI playback
* Full Echo recording
* Full Shadow playback
* Full Shadow recording
* 필요한 짧은 transition

기존 Billing 정책 자체는 변경하지 않는다.

---

# 39. 기존 P3 기능 조사

실제 구현 전에 현재 P3 코드에서 다음을 다시 확인한다.

1. P3 진입 함수
2. 현재 P3 Meaning Unit / Shadowing 경로
3. 현재 recorder
4. current temp recording path
5. DeviceFileSource replay
6. P3 완료 처리
7. exit cleanup
8. billing flags
9. 현재 UI 버튼
10. 기존 P3 녹음 삭제 정책

기존 구조를 최대한 재사용한다.

필요 없는 기존 동시낭독 경로는 새 P3 정상 동작 확인 후 정리한다.

---

# 40. 기존 P3와 dual execution 금지

기존 `_startP3MeaningUnitShadowing` 등의 동시낭독 경로와 새 P3가 동시에 실행되면 안 된다.

P3 한 번 진입 시 반드시 하나의 state machine만 동작해야 한다.

새 흐름 연결 후 구 경로의 호출 참조를 전체 검색한다.

---

# 41. P3 Breath Echo UI

최종 문장 전체를 계속 보여주되 현재 Breath 영역을 시각적으로 표시할 수 있는지 조사한다.

가능하면 BreathSegment 자체에는 text range 정보가 없으므로 억지로 음성 시간을 text token에 맞추지 않는다.

즉 정확한 text/audio forced alignment를 새로 만들지 않는다.

현재 Breath 번호:

`1 / 3`
`2 / 3`
`3 / 3`

정도만 표시해도 충분하다.

잘못된 단어 highlight를 만드는 것보다 낫다.

---

# 42. Full Echo UI

Breath Echoing 완료 후 화면 state가 명확히 바뀌어야 한다.

예:

`전체 문장을 들어보세요`

AI Original 재생.

↓

`이제 혼자 말해보세요`

User full recording.

녹음 완료 후:

`ECHO ▶`

활성.

제품 최종 문구는 기존 UI 언어에 맞춰 최소화한다.

---

# 43. Full Shadow UI

Shadow 시작 전에 3개 gap 선택 chip.

Tight / Normal / Relaxed.

선택 후:

`SHADOW START`

또는 기존 UI와 맞는 시작 동작.

Shadow 중에는 불필요한 버튼을 비활성화한다.

---

# 44. Compare UI

완료 후 UI를 지나치게 복잡하게 만들지 않는다.

최소:

AI ORIGINAL ▶
ECHO ▶
SHADOW ▶

그리고 필요하면 Shadow gap selector.

P3의 중심은 비교 청취다.

---

# 45. Full Echo 재시도

사용자가 Full Echo 결과가 마음에 들지 않으면 다시 녹음할 수 있는 현재 P3 UX가 있는지 조사한다.

있다면 재사용한다.

없다면 최소 `Retry` 정도만 검토한다.

재시도 시 이전 Echo temp file 삭제.

---

# 46. Full Shadow 재시도

Shadow도 다시 실행 가능하게 한다.

특히 gap을:

Tight
→ Normal
→ Relaxed

로 바꿔가며 연습할 수 있어야 한다.

재시도 때 이전 Shadow 녹음을 새 파일로 교체하고 오래된 temp 파일 삭제.

---

# 47. Shadow gap 비교 목적

이 옵션은 속도 변경이 아니다.

AI speech 자체 speed를 바꾸지 않는다.

Breath 사이 pause만 추가한다.

따라서:

Tight
Normal
Relaxed

모두 AI 단어 발음과 억양은 동일해야 한다.

오직 breathing room만 달라진다.

---

# 48. 새로운 API 호출 제한

P3 한 최종 sentence에서:

기본적으로 Smooth Jazz Full PCM TTS 1회.

그 뒤:

Breath replay
Full Echo Original replay
3종 Shadow gap

은 모두 동일 PCM의 로컬 재사용이어야 한다.

사용자가 Shadow를 10번 반복해도 TTS API 추가 호출이 없어야 한다.

---

# 49. Error recovery

TTS 실패:
→ 기존 retry/error UI를 재사용.

Mic permission 실패:
→ Breath User stage 또는 Full Echo/Shadow 시작 불가를 명확히 표시.

Recorder start 실패:
→ state machine이 다음 단계로 잘못 넘어가지 않게 한다.

Audio playback fail:
→ 무한 loading 금지.

stop/dispose 중 callback이 늦게 와도 generation guard로 무시한다.

---

# 50. 테스트

순수 함수 영역은 unit test를 만든다.

최소:

* gap 0 → original PCM 동일
* 300/500/800ms 삽입 길이 정확
* segment 순서 유지
* original sample 손실 없음
* one segment → unnecessary insertion 없음
* empty PCM graceful
* generation cancel 관련 가능한 순수 상태 test
* state transition helper가 있다면 정상 순서

기존 `buildGappedPcm()` 테스트가 있으면 중복 테스트를 만들지 말고 재사용/확장한다.

---

# 51. 실기기에서 가장 먼저 조정할 값

구조 구현 후 다음 값만 우선 미세 조정한다.

1. Breath trailing silence
   현재 약 1500ms 후보

2. Breath no-speech timeout
   현재 약 8s 후보

3. Full Echo final silence

4. Full Shadow tail
   현재 약 700ms 후보

5. Shadow gap:
   300 / 500 / 800ms

6. temporary Voice:
   cedar → 필요 시 marin/echo/verse 비교

나머지 구조는 먼저 고정한다.

---

# 52. P3 첫 실기기 검증 순서

1. P3 진입
2. Smooth Jazz Full PCM 준비
3. Breath 1/N AI 재생
4. AI 끝난 뒤 User mic 시작
5. 말하면 silence 후 다음 Breath
6. 마지막 Breath까지 정상
7. AI Original 전체 재생
8. AI가 완전히 끝난 후 User Full Echo 녹음
9. ECHO 재생 확인
10. Normal Shadow 선택
11. recorder 먼저 시작
12. gap이 들어간 AI와 동시에 Shadowing
13. 마지막까지 녹음
14. SHADOW 재생 확인
15. AI ORIGINAL / ECHO / SHADOW 각각 비교
16. Tight/Relaxed로 Shadow 재실행
17. gap만 달라지고 말하는 speed 자체는 같다는 것 확인
18. Back → 모든 소리/녹음 즉시 종료
19. 재진입 → User temp recording은 초기화, AI TTS는 cache reuse
20. Billing 잔여시간 정상 차감 확인

---

# 53. 성공 기준

P3 성공 기준은 기능 개수보다 학습 흐름이다.

사용자가 최종 문장을:

**작게 나누어 듣고 따라 하고**
→ **전체를 혼자 말하고**
→ **AI와 리듬을 맞춰 같이 말하고**
→ **세 결과를 직접 비교**

할 수 있어야 한다.

이 4단계가 자연스럽게 이어지면 성공이다.

---

# 54. 이번 작업 범위

이번에는 P3 구조 전체를 구현한다.

단:

* Pattern UI 추가하지 않음
* P2 Sentence Morphing 변경하지 않음
* 새로운 STT/GPT 평가 추가하지 않음
* 음성 정확도 점수 추가하지 않음
* 서버 업로드 없음
* Firebase schema 변경 없음
* 새로운 audio dependency 추가 없음

구조부터 완성한다.

---

# 55. 구현 후 반드시 보고

1. 신규/수정/삭제 파일
2. 기존 P3 경로와 무엇을 교체했는지
3. P3 state machine
4. Full PCM 생성/cache
5. Breath Echoing 연결
6. User Breath capture
7. Full Echo 흐름
8. Full Echo recording
9. Full Shadow 흐름
10. gap 3단계 구현
11. buildGappedPcm 재사용 여부
12. Shadow recording
13. AI/ECHO/SHADOW replay
14. Retry
15. Billing busy
16. cleanup
17. temp file lifetime
18. old P3 dead code 정리
19. tests
20. flutter analyze
21. 기존 P2/Circle/Scenario/Duo 무수정 여부
22. 실기기 QA 순서

---

# 56. 작업 후

구현 후 먼저 결과만 보고한다.

커밋하지 않는다.
빌드하지 않는다.
푸시하지 않는다.
배포하지 않는다.

내가 보고를 확인한 뒤 테스트 빌드 여부를 결정한다.


그런데 P2 읽기 시작을 안해.... P3는 완성문장과 세련 문장 2개가 있고 에코잉은 지금처럼 하면되고 쉐도잉은 한호흡 간격을 3단계로 설정해서 따라하게 해야해....폰 연결했으니 체크해 줘.