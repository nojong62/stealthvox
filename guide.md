# StealthVox 프로젝트 가이드 (Flutter)

## 📂 구조
- 화면(Pages): `lib/` 아래 이름별 폴더
- 커스텀 액션: `lib/custom_code/actions/`
- 커스텀 위젯: `lib/custom_code/widgets/`
- 전역 상태: `lib/app_state.dart`
- 테마: `lib/flutter_flow/flutter_flow_theme.dart`

`lib/custom_code/widgets/img/`는 디자인 참고용 이미지 폴더다. pubspec assets에 등록돼 있지
않고 Dart 코드에서도 참조하지 않으므로 빌드에 영향이 없다. 이 폴더가 지저분해도 무시할 것.

## ⚙️ 작업 규칙

### 코드
- 직접 Flutter 코드를 수정한다. (FlutterFlow 웹 에디터는 더 이상 쓰지 않음)
- `lib/flutter_flow/` 아래 생성 코드는 구조를 크게 바꾸지 말 것.
- 색상·폰트·간격은 `flutter_flow_theme.dart`의 테마 변수를 최우선으로 쓴다. 하드코딩 금지.

### 절차는 작업 크기에 맞춘다
모든 작업에 브랜치를 파고 백업 커밋을 만들 필요는 없다.

**작은 작업** — 한두 파일 수정, 버전 변경, 오타, 포맷, 빌드:
> 뭘 할지 한 줄 요약 → 수정 → `flutter analyze` → 보고

**일반 작업** — 기능 추가·수정:
> 계획 요약하고 동의 받기 → 관련 파일 분석 → 수정 → `flutter pub get` → `flutter analyze`
> → `git diff` 확인 → 수정 파일 목록·핵심 변경사항·남은 이슈 보고

**큰 작업** — 여러 화면에 걸치거나 되돌리기 어려운 변경:
> 위 절차 + 작업 브랜치 생성

### 백업 커밋
되돌리기 어려운 작업에서만 만든다. 만들 때는 **이번 작업과 관련된 파일만** 담을 것.
관계없는 작업 중인 파일을 "백업"이라며 같이 커밋하지 말 것 — 내 WIP를 멋대로 커밋하는 셈이다.

### 커밋 · 머지 · push
**요청받았을 때만** 한다. 알아서 main에 머지하거나 원격에 push하지 않는다.
단, 릴리스 빌드의 버전 변경은 출시본 추적이 끊기므로 커밋을 먼저 제안할 것.

### flutter analyze
이 저장소엔 warning/info가 600건 넘게 누적돼 있다 (대부분 생성 코드의 unused_import).
**error만 해결 대상**이다. 내 작업이 새로 만든 게 아닌 기존 warning은 건드리지 말 것.

## 📦 릴리스 빌드 (AAB)
- 버전: `pubspec.yaml`의 `version: 1.0.N+N` — versionName과 versionCode를 같이 올린다
- 빌드: `flutter build appbundle --release` (약 8분)
- 출력: `build/app/outputs/bundle/release/app-release.aab`
- 서명: `android/key.properties` + `android/app/code-stealth-vox-808wqy-keystore.jks`
  — 이미 설정 완료. 건드릴 필요 없다.
- 서명 확인은 AAB 안에 `META-INF/CODE-STE.RSA`가 있는지 보면 충분하다.
  버전 확인은 `base/manifest/AndroidManifest.xml`에서 versionName 문자열로 확인.
  그 이상 파고들지 말 것.

## ⚠️ 주의사항
- 기존 정상 작동 기능을 깨지 말 것
- 앱 실행/빌드 가능성을 최우선으로 할 것
- 불확실한 부분은 임의 삭제하지 말고 보고할 것
- 확인이 끝났으면 더 파지 말 것

================
지시문


# 무음 Deepgram 전송 차단 방법

핵심은 **WebSocket 연결을 무조건 끊는 것**이 아니라, 사람이 말하지 않을 때 **PCM 오디오 데이터를 보내지 않는 것**입니다.

## 권장 구조

### 1. 기기 내부 VAD로 먼저 판단

휴대폰에서 마이크 음량과 음성 여부를 먼저 판단합니다.

* 말소리가 시작되면 Deepgram 전송 시작
* 말이 끝난 뒤 약 500~800ms가 지나면 전송 중지
* AI 음성 재생 중에는 사용자 마이크 데이터를 기본 차단
* 사용자가 실제로 끼어들 때만 다시 전송

이미 StealthVox에 턴 감지와 barge-in 로직이 있으므로, 별도 대형 구조 변경보다 **Deepgram 송신 직전에 로컬 VAD 게이트를 추가**하는 방식이 적합합니다.

### 2. 앞부분이 잘리지 않도록 pre-roll 보관

VAD가 “말하기 시작”을 감지한 뒤 보내면 첫 자음이 잘릴 수 있습니다. 따라서 최근 **200~400ms 오디오를 메모리 버퍼에 임시 저장**했다가 음성이 감지되는 순간 함께 전송합니다.

흐름은 다음과 같습니다.

> 최근 300ms 임시 보관 → 음성 감지 → 임시 오디오+현재 오디오 전송

### 3. 말끝의 짧은 침묵은 바로 자르지 않기

한국어 사용자가 문장 중간에 잠시 쉬는 경우가 많으므로, 무음이 감지됐다고 즉시 중단하면 안 됩니다.

권장 기준:

* 0~500ms 무음: 계속 전송
* 약 600~900ms 무음: 발화 종료 후보
* 기존 semantic/end-of-turn 판정과 함께 최종 종료

### 4. AI 음성 재생 중 송신 제한

AI가 말하는 25분 동안 마이크 데이터를 Deepgram으로 보내면:

* AI 스피커 소리가 다시 인식됨
* 불필요한 Deepgram 비용 발생
* 가짜 사용자 발화가 생성됨

따라서 상태를 명확히 나눠야 합니다.

| 앱 상태            | Deepgram 전송  |
| --------------- | ------------ |
| 사용자 발화 대기       | VAD 감지 시만 전송 |
| 사용자 말하는 중       | 전송           |
| AI 생성 중         | 차단           |
| AI 음성 재생 중      | 기본 차단        |
| 사용자 barge-in 감지 | 즉시 재개        |
| 화면 백그라운드·pause  | 완전 중단        |

**연결 유지를 위한 소량의 keepalive 메시지와 실제 PCM 무음 전송은 구분해야 합니다.** 연결만 유지하면서 오디오 데이터를 보내지 않는 편이 좋습니다.

---

# TTS 글자 수 관리 방법

TTS-1은 **생성된 음성 시간 자체가 아니라 입력 글자 수**에 따라 과금됩니다. 현재 단가는 100만 글자당 $15입니다. ([OpenAI Developers][3])

## 1. 음성으로 읽을 텍스트만 TTS에 전달

모델 응답 전체를 보내지 말고, 실제로 소리 내 읽을 부분만 분리해야 합니다.

예를 들어 모델 결과가 다음처럼 구성된다면:

* 영어 대화문
* 한국어 번역
* 문법 설명
* 내부 제어 태그
* 학습 메타데이터

TTS에는 영어 대화문만 전달합니다.

다음 내용은 제거해야 합니다.

* `[CORRECTION]`, `[EVAPORATE]` 같은 제어 태그
* 화면 표시용 한국어 해설
* JSON 키와 값
* 같은 문장의 원문·번역 중복
* 발음기호와 디버그 문구
* 마크다운 기호

## 2. AI 답변 길이 상한 설정

25분 AI 발화는 대략 20,000자 전후입니다. AI 답변이 불필요하게 길어지면 TTS 비용과 대기시간이 함께 증가합니다.

대화 모드에서는 보통:

* 기본 응답: 1~2문장
* 설명 요청: 최대 3~4문장
* 문법 설명: 화면 텍스트 위주, 음성은 핵심만

으로 제한하는 것이 좋습니다.

예를 들어 긴 문법 설명 전체를 읽지 않고:

> “핵심은 과거형을 쓰는 것입니다. 자세한 내용은 화면을 보세요.”

정도만 음성으로 만들면 비용과 재생시간이 크게 줄어듭니다.

---

# TTS 중복 생성을 막는 방법

StealthVox에서는 비용 절감보다 **중복 재생·대기시간·턴 충돌 방지** 효과가 더 큽니다.

## 1. 각 AI 대사에 고유 utterance ID 부여

각 AI 응답마다 하나의 ID를 만들고 다음 상태를 기록합니다.

> 생성 전 → 생성 중 → 생성 완료 → 재생 중 → 재생 완료 → 취소

같은 ID가 이미 `생성 중` 또는 `완료`라면 새로운 TTS 요청을 보내지 않습니다.

## 2. 텍스트 해시로 동일 요청 차단

TTS 요청 전에 다음 항목을 묶어 식별값을 만듭니다.

* 정제된 문장
* 음성 종류
* 말하기 속도
* 언어

같은 식별값의 오디오가 이미 있으면 다시 생성하지 않고 기존 파일이나 메모리 캐시를 사용합니다.

예:

> 같은 문장 + 같은 목소리 + 같은 속도
> → TTS API 재호출 금지

## 3. 동시 요청은 하나만 허용

한 문장에 대해 다음 경로가 동시에 TTS를 요청할 수 있습니다.

* 스트리밍 청크 완료 이벤트
* 전체 응답 완료 이벤트
* 화면 rebuild
* 재생 큐 재시도
* prefetch 로직

첫 요청이 진행 중일 때 뒤 요청들은 새 API 호출을 만들지 않고 **첫 요청 결과를 공유**해야 합니다. 이를 흔히 single-flight 처리라고 합니다.

## 4. 취소된 응답은 재생 큐에 넣지 않기

사용자가 끼어들거나 `[CORRECTION]`이 발생하면:

* 진행 중 TTS 요청 취소
* 이미 받은 오디오 폐기 또는 캐시만 보관
* 재생 큐에서 해당 utterance ID 제거
* 이전 요청의 늦게 도착한 결과도 재생 금지

취소만 하고 늦게 도착한 응답을 막지 않으면, 이전 문장이 갑자기 재생되는 문제가 생깁니다.

## 5. 재생 재시도와 TTS 재생성을 분리

오디오 재생 실패가 발생했다고 TTS API를 다시 호출하면 안 됩니다.

우선순위는:

1. 기존 오디오 바이트로 재생 재시도
2. 로컬 캐시 확인
3. 데이터가 손상됐을 때만 TTS 재생성

이어야 합니다.

---

# 반드시 남겨야 할 비용 로그

세션별로 다음 네 항목만 기록하면 실제 1시간 비용을 거의 정확히 계산할 수 있습니다.

| 로그                      | 의미                     |
| ----------------------- | ---------------------- |
| `deepgram_audio_ms`     | 실제 Deepgram에 보낸 오디오 시간 |
| `tts_input_chars`       | TTS에 전달한 총 글자 수        |
| `tts_request_count`     | TTS API 호출 횟수          |
| `tts_duplicate_blocked` | 중복 차단 횟수               |

추가로 모델별 입력·출력 토큰도 저장하면 좋습니다.

> `gpt41_input/output_tokens`
> `gpt4o_mini_input/output_tokens`
> `realtime_audio_output_tokens`
